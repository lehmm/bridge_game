# Bridge Game

A physics-based bridge-building game made in Python (Pygame + Pymunk).

## How to run

1. Install Python 3.x
2. Install dependencies:
   pip install pygame pymunk
3. Run the game:
   python bridge_game.py
